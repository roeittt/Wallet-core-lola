# Base Module

The main code of this `base` module has been migrated to the `coin-base` package. The remaining code here is retained solely for internal use within the `crypto-lib` to avoid circular dependencies.

